function projectCreation([arg1,arg2]){
    let name=arg1;
    let hours=parseInt(arg2);

    let number=hours*3;
    console.log(`The architect ${name} will need ${number} hours to complete ${hours} project/s. `);
}